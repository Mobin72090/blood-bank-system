services:
  - type: web
    name: blood-bank-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: sqlite-data
      mountPath: /data/db
      sizeGB: 1
    envVars:
      - key: DB_PATH
        value: /data/db/database.db
